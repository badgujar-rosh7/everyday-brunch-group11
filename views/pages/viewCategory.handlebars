
<section class="section admin-section">
  <div class="container">
    <div class="row">
        <h1 class="main-heading text-center mt-5 mb-5">Admin</h1>
    </div>
    {{#if deleteerror}}
        <div class="accordion-item mb-5">
        <h2 class="accordion-header" id="panelsStayOpen-headingTwo">
        <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseTwo" aria-expanded="true" aria-controls="panelsStayOpen-collapseTwo">
            {{deleteerror}}
        </button>
        </h2>
    </div>
    <p><a href="./ViewCategory">Back to all Categorys</a></p>
    {{/if}}
    {{#if getCategory}}
    <div class="accordion-item mb-5">
        <h2 class="accordion-header" id="panelsStayOpen-headingTwo">
        <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseTwo" aria-expanded="true" aria-controls="panelsStayOpen-collapseTwo">
            View Category
        </button>
        </h2>
        <div id="panelsStayOpen-collapseTwo" class="accordion-collapse collapse show" aria-labelledby="panelsStayOpen-headingTwo">
            <div class="accordion-body ">      
              <table class="table table-striped table-dark table-hover">
                <tr>
                  <th>Category Name</th>
                  <th>Delete</th>
                </tr>
                {{#each getCategory}}
                <tr>
                  <td>{{category}}</td>
                  <td>
                    <form method="post" action="./deleteCategory">
                      <input type="text" name="deletecategory" value="{{category}}" hidden>
                      <input type="text" name="deleteid" value="{{_id}}" hidden>
                      <button class="btn btn-danger" type="submit"><span class="material-icons">delete</span></button>
                    </form>
                  </td>
                </tr>
                {{/each}}
              </table> 
            </div>
        </div>
    </div>
    {{/if}}

  </div>
</section>